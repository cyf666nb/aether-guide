# SCORE-IMPACT: Production-grade auth for admin + anonymous tourist flows.
"""Auth building blocks: JWT + password hashing + seed loader + dependencies.

All public API lives in sub-modules; importers should go through them directly.
"""
